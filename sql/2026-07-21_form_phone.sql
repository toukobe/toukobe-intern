-- 2026-07-21: フォーム（お問い合わせ・資料請求・早期/通常申し込み）に電話番号を追加
-- Supabase ダッシュボード → SQL Editor で実行してください（何度実行しても安全）。
-- コード側は電話番号を必須入力にしているが、この列が無くても送信が壊れないフォールバック入り
-- （列が無い環境では電話番号は管理者宛メールにのみ載る）。

ALTER TABLE form_submissions ADD COLUMN IF NOT EXISTS phone text; -- 連絡先電話番号（全フォーム必須）
